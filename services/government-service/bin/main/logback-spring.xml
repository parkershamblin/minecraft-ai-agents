<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Structured JSON to stdout: Loki-ready (M1), grep-able today.
         MDC (correlationId) is included in every line automatically. -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"government-service"}</customFields>
        </encoder>
    </appender>

    <root level="${LOG_LEVEL:-INFO}">
        <appender-ref ref="JSON"/>
    </root>
</configuration>
