name: dashboard
on:
  push:
    branches: [main]
    paths: ["apps/dashboard/**", "package-lock.json", ".github/workflows/_reusable-service.yml", ".github/workflows/dashboard.yml"]
  pull_request:
    paths: ["apps/dashboard/**", "package-lock.json", ".github/workflows/_reusable-service.yml", ".github/workflows/dashboard.yml"]
permissions:
  contents: read
  packages: write # never used (image: false) but the nested job requests it and validation is static
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  ci:
    uses: ./.github/workflows/_reusable-service.yml
    with:
      service: dashboard
      language: node
      workdir: apps/dashboard
      npm-workspace: "@civ/dashboard"
