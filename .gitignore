# dependencies / build output
node_modules/
dist/
build/
.next/
coverage/
.gradle/
__pycache__/
*.pyc
.venv/
venv/
.task/
*.tsbuildinfo

# local config & secrets — .env.example is the committed template
.env

# logs & OS noise
*.log
.DS_Store
Thumbs.db

# The local Minecraft server (world data + server jar) lives beside the repo
# but is not part of it — multi-GB world saves don't belong in git.
Minecraft 1.21.6 Server/
.coverage
