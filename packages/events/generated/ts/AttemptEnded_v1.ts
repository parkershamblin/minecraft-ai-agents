/* eslint-disable */
/**
 * GENERATED by packages/events/codegen/gen-ts.mjs — DO NOT EDIT.
 * Source of truth: packages/events/schemas. Regenerate with `task gen`.
 */

/**
 * A race attempt closed (ADR-10). 'won' fires when the win-condition milestone lands (first crafted iron pickaxe, occurredAt order, UUIDv7 tiebreak); the winning* fields then point back into the ledger so the victory claim is replayable evidence, not narration. The honestRace block carries the no-pollution assertion as recorded deltas over the attempt window — consumers (and the video captions) assert both are zero rather than trusting the footage. Producer: minecraft-service on world.events; aggregate is the Attempt.
 */
export interface AttemptEndedPayload {
  attemptId: string;
  /**
   * won = the win milestone landed; stalled = the harness's stall watchdog gave up (soak data, not failure noise); aborted = the operator ended the take.
   */
  outcome: "won" | "stalled" | "aborted";
  /**
   * null unless outcome is won.
   */
  winningTeamId: string | null;
  /**
   * null unless outcome is won.
   */
  winningVillagerId: string | null;
  /**
   * eventId of the winning ActionCompleted{craft:iron_pickaxe} — the ledger-proof pointer; null unless outcome is won.
   */
  winningEventId: string | null;
  /**
   * Wall-clock seconds from AttemptStarted to this event.
   */
  durationSeconds: number;
  honestRace: {
    /**
     * civ_llm_budget_tripped increase over the attempt window; an honest race requires 0 (a tripped breaker means FakeProvider was deliberating).
     */
    budgetTrippedDelta: number;
    /**
     * Count of fake-provider decisions over the attempt window; an honest race requires 0.
     */
    fakeProviderDelta: number;
  };
}
