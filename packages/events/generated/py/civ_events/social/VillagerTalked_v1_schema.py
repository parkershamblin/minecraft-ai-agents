# generated by datamodel-codegen:
#   filename:  social/VillagerTalked.v1.schema.json

from __future__ import annotations

from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, confloat, constr


class Position(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    x: float
    y: float
    z: float


class VillagerTalkedPayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    speakerId: UUID
    speakerName: constr(min_length=1)
    listenerIds: list[UUID] = Field(
        ...,
        description='Villagers the speaker believes are in earshot (from the world snapshot).',
    )
    message: constr(min_length=1)
    topic: str = Field(
        ...,
        description="Coarse conversational topic; 'smalltalk' until M1's enrichment.",
    )
    sentiment: confloat(ge=-1.0, le=1.0)
    location: Position
