# generated by datamodel-codegen:
#   filename:  government/VoteCast.v1.schema.json

from __future__ import annotations

from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, constr


class VoteCastPayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    electionId: UUID
    voterId: UUID
    candidateId: UUID = Field(..., description='The candidates-table row id.')
    candidateVillagerId: UUID = Field(
        ...,
        description="The villager voted for — consumers render 'Yara voted for Bram' without a government_db join.",
    )
    reason: constr(max_length=300) | None = Field(
        ..., description="The voter's stated why — episode gold; null when unstated."
    )
