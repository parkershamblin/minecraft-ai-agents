# generated by datamodel-codegen:
#   filename:  government/CandidateNominated.v1.schema.json

from __future__ import annotations

from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, constr


class CandidateNominatedPayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    electionId: UUID
    candidateId: UUID = Field(
        ...,
        description='The candidates-table row id (referenced by VoteCast.candidateId and ElectionDecided.winnerCandidateId).',
    )
    villagerId: UUID = Field(..., description='Who is running.')
    platform: constr(max_length=300) | None = Field(
        ..., description='The campaign promise; null for operator-seeded candidates.'
    )
