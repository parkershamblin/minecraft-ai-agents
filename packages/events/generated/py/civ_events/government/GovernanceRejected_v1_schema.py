# generated by datamodel-codegen:
#   filename:  government/GovernanceRejected.v1.schema.json

from __future__ import annotations

from enum import StrEnum
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field


class Action(StrEnum):
    declare_candidacy = 'declare_candidacy'
    vote = 'vote'


class ErrorCode(StrEnum):
    WINDOW_CLOSED = 'WINDOW_CLOSED'
    ALREADY_VOTED = 'ALREADY_VOTED'
    ALREADY_A_CANDIDATE = 'ALREADY_A_CANDIDATE'
    NOT_A_CANDIDATE = 'NOT_A_CANDIDATE'
    UNKNOWN_ELECTION = 'UNKNOWN_ELECTION'
    STALE_COMMAND = 'STALE_COMMAND'
    INVALID_PARAMS = 'INVALID_PARAMS'


class GovernanceRejectedPayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    commandId: UUID
    villagerId: UUID = Field(..., description='Whose request was refused.')
    action: Action
    electionId: UUID | None = Field(
        ...,
        description='The election named in the request; null when the request never named a resolvable one.',
    )
    errorCode: ErrorCode = Field(
        ...,
        description="Machine-readable cause; the same vocabulary government-service's REST plane uses in problem+json.",
    )
    message: str = Field(
        ...,
        description='Prescriptive, villager-readable prose (the M2-1 lesson: diagnosis quality IS competence — this string becomes the percept).',
    )
