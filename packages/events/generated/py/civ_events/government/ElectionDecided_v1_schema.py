# generated by datamodel-codegen:
#   filename:  government/ElectionDecided.v1.schema.json

from __future__ import annotations

from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, conint


class ElectionDecidedPayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    electionId: UUID
    winnerCandidateId: UUID
    winnerVillagerId: UUID = Field(..., description='The new mayor.')
    voteCounts: dict[str, conint(ge=0)] = Field(
        ...,
        description='candidateVillagerId -> votes received. Candidates with zero votes appear with 0.',
    )
    totalVotes: conint(ge=1)
