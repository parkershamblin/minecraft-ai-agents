# generated by datamodel-codegen:
#   filename:  agent/DecisionMade.v1.schema.json

from __future__ import annotations

from enum import StrEnum
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, conint


class LlmProvider(StrEnum):
    openai = 'openai'
    ollama = 'ollama'
    fake = 'fake'


class DecisionMadePayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    villagerId: UUID
    decision: str = Field(
        ..., description='Human-readable statement of what the villager chose to do.'
    )
    reasoning: str = Field(
        ...,
        description="The LLM's stated rationale — episode gold and debugging gold alike.",
    )
    llmProvider: LlmProvider
    llmModel: str
    tokensUsed: conint(ge=0)
    latencyMs: conint(ge=0)
    error: bool | None = Field(
        False,
        description='True when the LLM output failed schema validation and the tick fell back to idle (see CIV-7).',
    )
