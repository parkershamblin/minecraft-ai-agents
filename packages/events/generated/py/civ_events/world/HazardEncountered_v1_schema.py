# generated by datamodel-codegen:
#   filename:  world/HazardEncountered.v1.schema.json

from __future__ import annotations

from enum import StrEnum
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field


class Phase(StrEnum):
    trapped = 'trapped'
    escaped = 'escaped'
    escape_failed = 'escape_failed'


class Position(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    x: float
    y: float
    z: float


class HazardEncounteredPayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    villagerId: UUID
    hazardType: str = Field(
        ...,
        description="The concrete hazard, named after the world feature or condition: powder_snow (the escape reflex) and starvation (the eat reflex's crisis — trapped when starving AND helpless, escaped on recovery, escape_failed never emitted); lava, fire, drowning are future values.",
    )
    phase: Phase = Field(
        ...,
        description='trapped = hazard detected around the body; escaped = the reflex freed the villager; escape_failed = the reflex gave up this round (it will retry).',
    )
    position: Position
    detail: str | None = Field(
        ...,
        description='Human-readable color for prompts and the ledger: blocks dug, seconds stuck, failure reason.',
    )
