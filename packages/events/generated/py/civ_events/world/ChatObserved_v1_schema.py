# generated by datamodel-codegen:
#   filename:  world/ChatObserved.v1.schema.json

from __future__ import annotations

from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, constr


class Position(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    x: float
    y: float
    z: float


class ChatObservedPayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    villagerId: UUID | None = Field(
        ...,
        description="The speaker's villagerId resolved via the mc:roster hash; null when the speaker is not a roster villager (e.g. a human player).",
    )
    speakerUsername: constr(min_length=1) = Field(
        ...,
        description='The raw in-game username of the speaker — always present, even when villagerId is null.',
    )
    message: str
    heardByIds: list[UUID] = Field(
        ..., description='Roster villagers in earshot (excluding the speaker).'
    )
    position: Position
