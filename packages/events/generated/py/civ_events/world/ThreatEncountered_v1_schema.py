# generated by datamodel-codegen:
#   filename:  world/ThreatEncountered.v1.schema.json

from __future__ import annotations

from enum import Enum, StrEnum
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, confloat, conint


class Phase(StrEnum):
    spotted = 'spotted'
    engaged = 'engaged'
    killed = 'killed'
    escaped = 'escaped'
    overwhelmed = 'overwhelmed'


class Response(Enum):
    fight = 'fight'
    flee = 'flee'
    NoneType_None = None


class Position(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    x: float
    y: float
    z: float


class ThreatEncounteredPayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    villagerId: UUID
    threatType: str = Field(
        ...,
        description="The nearest hostile's entity name (zombie, skeleton, creeper, ...). Free string — new mobs must not need a contract commit.",
    )
    phase: Phase = Field(
        ...,
        description="spotted = episode opened (the mind's chance to preempt); engaged = a maneuver started or the response flipped; killed = the threat died and the episode closed; escaped = the body broke contact and the episode closed; overwhelmed = the maneuver is not working (cornered, out-damaged) — the one moment only the mind can change the plan.",
    )
    response: Response | None = Field(
        ...,
        description="The body's current maneuver; null before one is chosen (spotted) or when none applies.",
    )
    count: conint(ge=1) = Field(
        ..., description='Hostiles tracked in the episode at emission time.'
    )
    distance: confloat(ge=0.0) = Field(
        ..., description='Blocks to the nearest tracked hostile at emission time.'
    )
    position: Position
    detail: str | None = Field(
        ...,
        description='Human-readable color for prompts and the ledger: what attacked, how the fight ended, why the maneuver failed.',
    )
