# generated by datamodel-codegen:
#   filename:  world/VillagerSpawned.v1.schema.json

from __future__ import annotations

from enum import StrEnum
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, constr


class SpawnReason(StrEnum):
    seed = 'seed'
    respawn = 'respawn'
    reconnect = 'reconnect'


class Position(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    x: float
    y: float
    z: float


class VillagerSpawnedPayload(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    villagerId: UUID
    name: constr(min_length=1)
    position: Position
    spawnReason: SpawnReason = Field(
        ...,
        description='Why the bot session started: initial seeding, death respawn, or connection recovery.',
    )
